document.addEventListener("DOMContentLoaded", function () {
    initModal(
        // ".modal-consultation",
        // ".seller-hero__button",
        // ".modal-consultation__close"
    );

    initModal(
        ".modal-consultation",
        ".problem-two__button",
        ".modal-consultation__close"
    );
});
