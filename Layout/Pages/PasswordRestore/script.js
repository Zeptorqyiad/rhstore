document.addEventListener('DOMContentLoaded', function() {
    const form = document.querySelector('.password-restore__form');
    const passwordField = document.querySelector('.password-restore__input-pass input');
    const passwordCheckField = document.querySelector('.password-restore__input-check input');
    const submitButton = document.querySelector('.password-restore__submit');

    form.addEventListener('submit', function(event) {
        const formData = new FormData(form);

    });
});